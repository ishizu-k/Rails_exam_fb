<h3>以下の内容で投稿しますか？</h3>

<%= form_with(model: @record, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>本文　<%= @record.content %></p>
  <%= form.submit  "投稿する" %>
<% end %>

<%= form_with(model: @record, local: true, url: back_confirm, method: 'get') do |form| %>
  <%= form.hidden_field :content %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
