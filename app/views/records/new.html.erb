<h2>投稿する</h2>
<%= form_with(model: @record, local: true) do |form| %>
  <div class="record_title">
    <%= form.label :本文 %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
