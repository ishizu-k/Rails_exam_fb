<h2>投稿一覧</h2>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @records.each do |record| %>
  <tr>
    <td><%= record.content %></td>
    <td><%= link_to "編集", edit_record_path(record.id) %></td>
  </tr>
<% end %>
</table>

<%= link_to '新規投稿', new_record_path %>
